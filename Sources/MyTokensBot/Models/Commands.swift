import Foundation

struct Commands {
    static let main = "main"
    static let start = "start"
    static let stop = ["❌ Stop", "stop"]
    static let help = ["ℹ️ Help", "help"]
    static let add = ["➕ Add", "add"]
    static let cancel = ["❌ Cancel", "cancel"]
}
